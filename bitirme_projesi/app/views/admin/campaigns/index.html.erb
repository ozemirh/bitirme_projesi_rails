<div class="flex items-center justify-between mb-6">
  <h1 class="text-2xl font-bold tracking-tight text-white">CAMPAIGNS</h1>
  <%= link_to "+ New Campaign", new_admin_campaign_path,
        class: "text-sm bg-indigo-600 hover:bg-indigo-500 text-white px-4 py-2 font-bold rounded-lg transition-all" %>
</div>

<div class="rounded-xl bg-slate-900 border border-slate-800 overflow-hidden">
  <table class="w-full text-sm">
    <thead class="text-slate-400 text-xs uppercase">
      <tr>
        <th class="px-5 py-3 text-left">Name</th>
        <th class="px-5 py-3 text-left">Language</th>
        <th class="px-5 py-3 text-left">Strategy</th>
        <th class="px-5 py-3 text-left">Status</th>
        <th class="px-5 py-3 text-right">Sent</th>
        <th class="px-5 py-3 text-right">Captured</th>
        <th class="px-5 py-3"></th>
      </tr>
    </thead>
    <tbody>
      <% @campaigns.each do |c| %>
        <tr class="border-t border-slate-800 hover:bg-slate-800/50">
          <td class="px-5 py-3"><%= link_to c.name, admin_campaign_path(c), class: "text-indigo-400 hover:underline" %></td>
          <td class="px-5 py-3 text-slate-400 font-medium"><%= c.email_language %></td>
          <td class="px-5 py-3 text-slate-400"><%= c.prompt_type.capitalize %></td>
          <td class="px-5 py-3">
            <span class="text-xs px-2 py-0.5 rounded-full
              <%= c.status == 'sent' ? 'bg-emerald-500/15 text-emerald-300' : 'bg-slate-700 text-slate-300' %>">
              <%= c.status %>
            </span>
          </td>
          <td class="px-5 py-3 text-right"><%= c.emails_sent %></td>
          <td class="px-5 py-3 text-right text-rose-400"><%= c.creds_captured %></td>
          <td class="px-5 py-3 text-right">
            <%= link_to "Edit", edit_admin_campaign_path(c), class: "text-xs text-slate-400 hover:text-white" %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
